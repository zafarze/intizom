План исправления (4 файла)
1. settings.py — подключить Django Cache Framework к Redis
Redis уже есть как REDIS_URL, просто добавить блок CACHES по тому же паттерну. Никаких новых зависимостей.

2. analytics.py — оптимизировать тренд: 12 запросов → 1
Заменить цикл одним запросом через TruncMonth + условный Sum (стандартный Django ORM).

3. analytics.py — кешировать ответы
StatisticsView → 5 минут TTL
DashboardStatsView → 2 минуты TTL
4. logs.py — инвалидировать кеш
При добавлении/удалении нарушения → cache.delete_many(...) — данные сразу актуальны.

Ожидаемый результат
До	После
Запросов на Statistics	~20	0 (кеш) / 7–8 (первый раз)
Запросов на тренд	12	1
Время загрузки Statistics	2–5 с	< 100 мс
Приступить к реализации?